// 1. Importe o módulo e atribua-o a uma variável (usando o require)
//const prompt = require('prompt-sync')();
const prompt = require('prompt-sync')();
const primeiroNome = prompt("Digite seu primeiro nome: ");
console.log(`Olá, ${primeiroNome}! Bem-vindo ao sistema de análise de repasses financeiros.`);
console.log("--------------------------------------------------");

